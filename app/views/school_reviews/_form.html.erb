<%= form_for(@school_review) do |f| %>
  <% if @school_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@school_review.errors.count, "error") %> prohibited this school_review from being saved:</h2>

      <ul>
      <% @school_review.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rating %><br />
    <%= f.number_field :rating %>
  </div>
  <div class="field">
    <%= f.label :comments %><br />
    <%= f.text_area :comments %>
  </div>
  <div class="field">
    <%= f.label :school_id %><br />
    <%= f.number_field :school_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
